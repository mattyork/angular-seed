basePath = '../../';

files = [
  ANGULAR_SCENARIO,
  ANGULAR_SCENARIO_ADAPTER,
  
  'test/e2e/scenarios.js'
  // 'test/unit/*.js'
];

autoWatch = true;

proxies = {
  '/': 'http://localhost:8000/'
};

browsers = ['Chrome'];

singleRun = true;
